name: Test

on: 
  push: 
    branches: [ main ]

jobs:
  test: 
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - run: npm install
      - run: npm run test

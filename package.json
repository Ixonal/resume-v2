{
  "name": "resume-v2",
  "version": "0.0.1",
  "description": "",
  "repository": {
    "type": "git"
  },
  "type": "module", 
  "workspaces": {
    "packages": [
      "packages/resume-infrastructure",
      "packages/*"
    ]
  },
  "scripts": {
    "start": "build",
    "build": "npm run build --workspaces",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.5"
  }
}
